<%= form_with(model: [@product, Rating.new]) do |f| %>
  <div class="field">
    <%= f.label :score, "Rating" %>
    <%= f.number_field :score, min: 1, max: 5, step: 1, value: 1 %>
  </div>
  <div class="actions">
    <%= f.submit "Submit Rating", class: "btn btn-primary" %>
  </div>
<% end %>